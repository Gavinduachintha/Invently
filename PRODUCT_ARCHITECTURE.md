# Product Module Architecture

## Component Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                        Dashboard.jsx                         │
│  (Main container - handles navigation between views)         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ (when currentView === "products")
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      ProductsView.jsx                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Header Section                                        │  │
│  │  - Title: "Products"                                   │  │
│  │  - Description                                         │  │
│  │  - [Add Product Button] → Opens Modal                 │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Statistics Cards                                      │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐           │  │
│  │  │  Total   │  │In Stock  │  │Low Stock │           │  │
│  │  │ Products │  │ Products │  │ Products │           │  │
│  │  └──────────┘  └──────────┘  └──────────┘           │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Search Bar                                            │  │
│  │  🔍 [Search products by name, SKU, or category...]    │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Products Table                                        │  │
│  │  ┌────────┬──────┬──────────┬───────┬───────┬──────┐ │  │
│  │  │Product│ SKU  │ Category │ Stock │ Price │Status│ │  │
│  │  ├────────┼──────┼──────────┼───────┼───────┼──────┤ │  │
│  │  │ Data   │ Data │  Data    │ Data  │ Data  │Badge │ │  │
│  │  │        │      │          │       │       │[✏️][🗑️]│ │  │
│  │  └────────┴──────┴──────────┴───────┴───────┴──────┘ │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  Data Source: useProducts() hook ──────────────────┐        │
└────────────────────────────────────────────────────┼────────┘
                                                      │
                    ┌─────────────────────────────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │   useProducts.js      │
        │   (Custom Hook)       │
        │                       │
        │  - Fetches products   │
        │  - Manages state      │
        │  - Error handling     │
        └───────────┬───────────┘
                    │
                    │ Appwrite SDK
                    ▼
        ┌───────────────────────┐
        │   Appwrite Database   │
        │   "products"          │
        │   Collection          │
        └───────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│              Add Product Modal (Overlay)                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                AddProduct.jsx                          │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Add Product                              [X]    │  │  │
│  │  ├─────────────────────────────────────────────────┤  │  │
│  │  │  Product Name: [____________] SKU: [_______]    │  │  │
│  │  │  Category: [____________] Stock: [_______]      │  │  │
│  │  │  Price: [____________] Status: [▼ Dropdown]     │  │  │
│  │  │  Description:                                    │  │  │
│  │  │  [_________________________________________]     │  │  │
│  │  │                                                  │  │  │
│  │  │             [Cancel]  [Add Product]             │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │                                                        │  │
│  │  Triggered by: "Add Product" button                   │  │
│  │  Closed by: onClose() callback                        │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

```
┌──────────────┐
│   User       │
└──────┬───────┘
       │
       │ 1. Clicks "Products" in sidebar
       ▼
┌──────────────────┐
│  Dashboard.jsx   │
│  setCurrentView  │
│   ("products")   │
└──────┬───────────┘
       │
       │ 2. Renders ProductsView
       ▼
┌──────────────────┐
│ ProductsView.jsx │
│                  │
│ useProducts() ───┼───► 3. Fetches from Appwrite
│      │           │
│      └───────────┼───► 4. Receives products[]
│                  │
│  [Search box] ───┼───► 5. Filters products locally
│                  │
│  [Table] ────────┼───► 6. Displays filtered results
│                  │
└──────┬───────────┘
       │
       │ 7. User clicks "Add Product"
       ▼
┌──────────────────┐
│ setShowModal     │
│   (true)         │
└──────┬───────────┘
       │
       │ 8. Shows modal
       ▼
┌──────────────────┐
│ AddProduct.jsx   │
│                  │
│ - User fills form│
│ - Clicks Submit  │──► 9. Saves to Appwrite (TODO)
│                  │
│ onClose() ───────┼───► 10. Closes modal
│                  │
└──────┬───────────┘
       │
       │ 11. Refresh/Refetch (TODO)
       ▼
┌──────────────────┐
│  Updated table   │
└──────────────────┘
```

## Module Structure

```
Products Module
│
├── Data Layer (Model)
│   └── useProducts.js
│       ├── Fetches data from Appwrite
│       ├── Manages loading state
│       ├── Handles errors
│       └── Returns: { products, error, loading }
│
├── Presentation Layer (View)
│   ├── ProductsView.jsx
│   │   ├── Statistics cards
│   │   ├── Search functionality
│   │   ├── Data table
│   │   └── Action buttons
│   │
│   └── AddProduct.jsx
│       ├── Form inputs
│       ├── Validation
│       ├── Submit handler
│       └── Modal UI
│
└── Integration Layer (Controller)
    └── Dashboard.jsx
        ├── Navigation
        ├── Route handling
        └── View rendering
```

## Comparison: Suppliers vs Products

```
┌─────────────────────────┬─────────────────────────┐
│      SUPPLIERS          │       PRODUCTS          │
├─────────────────────────┼─────────────────────────┤
│ useSuppliers.js         │ useProducts.js          │
│ SuppliersView.jsx       │ ProductsView.jsx        │
│ Addsupplier.jsx         │ AddProduct.jsx          │
│                         │                         │
│ Same Pattern ────────────── Same Pattern         │
│ Same Structure ──────────── Same Structure        │
│ Same Style ──────────────── Same Style            │
└─────────────────────────┴─────────────────────────┘
```

## Benefits of This Architecture

✅ **Separation of Concerns**

- Data fetching (hooks)
- UI rendering (components)
- Business logic (separate)

✅ **Reusability**

- Hook can be used in multiple components
- Form component can be reused for edit

✅ **Testability**

- Each piece can be tested independently
- Mock data sources easily

✅ **Maintainability**

- Clear file structure
- Easy to find and fix issues
- Consistent patterns

✅ **Scalability**

- Easy to add new features
- Simple to extend functionality
- Modular design
